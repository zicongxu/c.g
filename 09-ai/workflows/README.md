# AI Workflows

Each workflow lives at `workflows/<workflow-name>/` and describes triggers, state transitions, people, agents, tools, approvals, idempotency, retries, data destinations, audit events, failure recovery, and human takeover.

Register every reusable workflow in `registry.yaml`.
