# Engineering

- `architecture/`：当前系统地图、数据流和接口边界。
- `decisions/`：按编号保存 ADR，记录不可轻易逆转的决定。
- `standards/`：代码、API、安全、数据和发布规范。
- `runbooks/`：部署、回滚、故障排查和日常操作。
- `incidents/`：事故时间线、影响、根因和改进项。
- `repositories/`：代码仓库、owner、环境与文档入口索引。

代码细节留在代码仓库；这里保存跨仓库结构、约束、决策和操作知识。
