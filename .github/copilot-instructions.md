# Repository Instructions

Before suggesting or making changes, read and follow `/AGENTS.md`.

Use `/INDEX.md` for discovery and `/knowledge-map.yaml` for artifact routing. Load only the relevant domain README and registry after selecting a domain. Never place a known artifact type in `/00-inbox/`.
